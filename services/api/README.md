Service API (FastAPI)

Endpoints:
- POST /ingest  -> ingest readings
- GET /health   -> health check
- POST /predict -> predict crops (provide farm_id or features)

Environment: set DATABASE_URL and MODEL_PATH
